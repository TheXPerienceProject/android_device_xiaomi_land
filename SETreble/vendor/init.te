allow init { domain -lmkd -crash_dump }:process noatsecure;
allow init adsprpcd_file:filesystem { relabelto relabelfrom associate mount getattr };
allow init cameraserver:fd use;
allow init debugfs:file write;
allow init fpc_sysfs:file { read write open };
allow init graphics_device:chr_file write;
allow init gx_fpd:binder { call transfer };
allow init hal_camera_default:fd use;
allow init hal_graphics_allocator_default:fd { use };
allow init hal_dumpstate_impl_exec:file { execute };
allow init hidl_base_hwservice:hwservice_manager add;
allow init hwservicemanager:binder { call transfer };
allow init ion_device:chr_file { read open ioctl write };
allow init init:udp_socket ioctl;
allow init logd:dir getattr;
allow init modem_efs_partition_device:blk_file write;
allow init netd:unix_stream_socket connectto;
allow init netd_socket:sock_file write;
allow init proc:file getattr;
allow init property_socket:sock_file write;
allow init persist_file:filesystem { associate mount relabelfrom relabelto unmount };
allow init sdcardfs:dir { search };
allow init servicemanager:binder call;
allow init self:capability { net_bind_service sys_module };
allow init self:capability2 block_suspend;
allow init self:netlink_generic_socket { read write };
allow init self:netlink_kobject_uevent_socket read;
allow init self:netlink_kobject_uevent_socket { bind create setopt };
allow init self:netlink_route_socket { read bind create getattr nlmsg_read nlmsg_write setopt write };
allow init self:netlink_socket { bind create read write };
allow init self:rawip_socket { create getopt setopt };
allow init self:socket create_socket_perms_no_ioctl;
allow init self:tcp_socket { create ioctl };
allow init self:udp_socket ioctl;
allow init servicemanager:binder { call transfer };
allow init socket_device:sock_file { create setattr };
allow init sysfs:file setattr;
allow init sysfs_leds:lnk_file read;
allow init system_data_file:sock_file write;
allow init system_server:fd use;
allow init system_server:unix_stream_socket { read write };
allow init tee_device:chr_file { write read open ioctl setattr };
allow init uio_device:chr_file write;
allow init vfat:file { getattr open read };
allow init vfat:dir { open read };
allow init video_device:chr_file { ioctl write };
allow init wcnss_device:chr_file { open read write };
allow init vendor_file:file { read getattr execute };

# Requied to load shim libs
allow init cameraserver:process { noatsecure };
allow init hal_camera:process { noatsecure };
allow init mm-qcamerad:process { noatsecure };
