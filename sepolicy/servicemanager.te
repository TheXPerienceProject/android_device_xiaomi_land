allow servicemanager init:dir search;
allow servicemanager init:file { open read };
allow servicemanager init:process getattr;
allow servicemanager fps_hal:dir search;
allow servicemanager fps_hal:file { read open };
allow servicemanager fps_hal:process getattr;
